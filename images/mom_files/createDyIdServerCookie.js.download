/******/ (function() { // webpackBootstrap
/******/ 	"use strict";
var __webpack_exports__ = {};
/*!********************************************************************************************!*\
  !*** ./cartridges/app_mk_storefront/cartridge/client/default/js/createDyIdServerCookie.js ***!
  \********************************************************************************************/


$(window).on('load', function () {
  var name = '_dyid=';
  var decodedCookie = decodeURIComponent(document.cookie);
  var allcookies = decodedCookie.split(';');
  var newCookieValue = '';
  var cookiePresent = false;
  var isDynamicYieldEnabled = window.isDynamicYieldEnabled || false;

  if (isDynamicYieldEnabled) {
    for (var i = 0; i < allcookies.length; i++) {
      var cookie = allcookies[i];

      while (cookie.charAt(0) === ' ') {
        cookie = cookie.substring(1);
      }

      if (cookie.indexOf('_dyid_server') === 0) {
        cookiePresent = true;
      }
    }

    if (!cookiePresent) {
      for (var _i = 0; _i < allcookies.length; _i++) {
        var _cookie = allcookies[_i];

        while (_cookie.charAt(0) === ' ') {
          _cookie = _cookie.substring(1);
        }

        if (_cookie.indexOf(name) === 0) {
          newCookieValue = _cookie.substring(name.length, _cookie.length);
        }
      }

      if (newCookieValue !== '') {
        var d = new Date();
        d.setTime(d.getTime() + 365 * 24 * 60 * 60 * 1000);
        var expires = 'expires=' + d.toUTCString();
        document.cookie = '_dyid_server=' + newCookieValue + ';' + expires + '; Path=/;';
      }
    }
  }
});
/******/ })()
;
//# sourceMappingURL=createDyIdServerCookie.js.map